name: Build and Deploy

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Execute Deployment Scripts
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            set -ex
            
            # ========== 初始化设置 ==========
            echo "🚀 Starting deployment process..."
            BRANCH="main"
            REPO_DIR="/www/cicd_wuyu/wuyuProject/WuYuEducation"
            cd "$REPO_DIR" || { echo "❌ Cannot access $REPO_DIR"; exit 1; }
            
            # ========== 代码同步 ==========
            echo "🔄 Syncing $BRANCH branch..."
            git reset --hard HEAD
            git checkout -f $BRANCH
            git pull origin $BRANCH --rebase
            
            # ========== 配置设置 ==========
            SERVER_PORT=9080
            FRONTEND_PORT=9180
            BACKEND_JAR_NAME="wuyu-server-main.jar"
            FRONTEND_IMAGE="wuyu-front-main"
            BACKEND_LOG="backend-main.log"
            
            # ========== 目录验证 ==========
            echo "🔍 Verifying project structure..."
            if [ ! -d "wuyu-front" ]; then
              echo "❌ wuyu-front directory missing!"
              echo "Directory contents:"
              ls -la
              exit 1
            fi
            
            # ========== 前端部署 ==========
            echo "📂 Processing frontend..."
            cd wuyu-front || { echo "❌ Cannot enter wuyu-front directory"; exit 1; }
            
            # 安全清理（保留 Docker 构建必需的文件）
            echo "🧹 Cleaning workspace (preserving Docker build files)..."
            # 保留所有 Git 跟踪文件和目录
            git clean -fdx -e 'Dockerfile*' -e 'package*.json' -e 'src/' -e 'public/' || true
            
            echo "📁 Current directory contents:"
            ls -la
            
            # 确认 Dockerfile 存在
            if [ ! -f "Dockerfile" ]; then
              echo "❌ ERROR: Dockerfile missing in wuyu-front directory!"
              echo "Please ensure Dockerfile is committed to the repository"
              exit 1
            fi
            
            # 禁用缓存构建镜像
            echo "🏗️ Building Frontend (no-cache)..."
            docker build \
              --no-cache \
              --pull \
              -t $FRONTEND_IMAGE . || { 
                echo "❌ Frontend build failed"; 
                echo "Build context contents:"; 
                ls -la .; 
                exit 1; 
              }
            
            # 部署前端容器
            echo "🚀 Deploying Frontend container..."
            docker stop $FRONTEND_IMAGE >/dev/null 2>&1 || true
            docker rm $FRONTEND_IMAGE >/dev/null 2>&1 || true
            docker run -d \
              --name $FRONTEND_IMAGE \
              -p $FRONTEND_PORT:80 \
              $FRONTEND_IMAGE || { echo "❌ Frontend deployment failed"; exit 1; }
            
            # ========== 后端部署 ==========
            echo "📂 Processing backend..."
            cd ../wuyu-server || { echo "❌ Cannot enter wuyu-server directory"; exit 1; }
            
            # 清理后端工作区
            git clean -fdx -e 'pom.xml' -e 'src/'
            
            # 停止现有进程
            echo "🛑 Stopping existing backend..."
            PID=$(lsof -t -i:$SERVER_PORT || true)
            if [ ! -z "$PID" ]; then
              kill -9 $PID
              sleep 3
            fi
            
            # 构建项目
            echo "🔧 Building backend with Maven..."
            mvn clean package -DskipTests || { 
                echo "❌ Backend build failed";
                echo "===== MVEN BUILD LOG ====="
                cat target/surefire-reports/*.txt || true;
                exit 1;
            }
            
            mv target/*.jar target/$BACKEND_JAR_NAME
            rm -f $BACKEND_LOG
            
            # 启动应用
            echo "🚀 Starting backend on port $SERVER_PORT..."
            nohup java -jar target/$BACKEND_JAR_NAME --server.port=$SERVER_PORT > $BACKEND_LOG 2>&1 &
            BACKEND_PID=$!
            echo "📝 Backend PID: $BACKEND_PID"
            
            # 健康检查
            echo "🩺 Waiting for backend to start..."
            for i in {1..10}; do
              if curl -sSf http://localhost:$SERVER_PORT/actuator/health >/dev/null; then
                echo "✅ Backend is up and running!"
                break
              else
                echo "⏳ Waiting for backend startup ($i/10)..."
                sleep 5
              fi
              
              if [ $i -eq 10 ]; then
                echo "❌ Backend failed to start within 50 seconds"
                echo "===== BACKEND LOGS ====="
                cat $BACKEND_LOG
                echo "===== PROCESS LIST ====="
                ps aux | grep java
                exit 1
              fi
            done
            
            # ========== 清理和总结 ==========
            echo "🧹 Cleaning Docker resources..."
            docker system prune -af --filter "until=48h"
            
            echo "✅ Deployment completed successfully!"
            echo "Frontend URL: http://${{ secrets.SERVER_HOST }}:$FRONTEND_PORT"
            echo "Backend URL: http://${{ secrets.SERVER_HOST }}:$SERVER_PORT"
