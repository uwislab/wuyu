# name: Build and Deploy

# on:
#   push:
#     branches: [ "main" ]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       # 步骤1：检出代码
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       # 步骤2：通过SSH执行部署操作
#       - name: Execute Deployment Scripts
#         uses: appleboy/ssh-action@v1
#         with:
#           host: ${{ secrets.SERVER_HOST }}
#           username: ${{ secrets.SERVER_USER }}
#           key: ${{ secrets.SERVER_SSH_KEY }}
#           script: |
#             set -e  # 任何命令失败立即终止脚本
            
#             # 初始化变量
#             BRANCH="main"
#             REPO_DIR="/www/cicd_wuyu/wuyuProject/WuYuEducation"
#             cd "$REPO_DIR"

#             # ========== 代码同步阶段 ==========
#             echo "🔄 Syncing main branch..."
#             git checkout -f main
#             git pull origin main

#             # ========== 端口计算阶段 ==========
#             GROUP_NUMBER=0
#             SERVER_PORT=9080
#             FRONTEND_PORT=9180
            
#             # 命名后端JAR和前端镜像
#             BACKEND_JAR_NAME="wuyu-server-main.jar"
#             FRONTEND_IMAGE="wuyu-front-main"
#             BACKEND_LOG="backend-main.log"

#             # ========== 目录验证 ==========
#             echo "🔍 Verifying project directory structure..."
#             if [ ! -d "wuyu-server" ]; then
#               echo "❌ wuyu-server directory not found in $REPO_DIR"
#               echo "Current directory contents:"
#               ls -la
#               exit 1
#             fi
            
#             if [ ! -d "wuyu-front" ]; then
#               echo "❌ wuyu-front directory not found in $REPO_DIR"
#               echo "Current directory contents:"
#               ls -la
#               exit 1
#             fi

#             # ========== 前端处理阶段 ========== 
#             echo "📂 Moving to frontend directory..."
#             cd wuyu-front
#             pwd  # 打印当前工作目录，用于调试
            
#             echo "🏗️ Building Frontend..."
#             docker build -t $FRONTEND_IMAGE . || { echo "❌ Frontend build failed"; exit 1; }

#             echo "🚀 Deploying Frontend..."
#             docker stop $FRONTEND_IMAGE >/dev/null 2>&1 || true
#             docker rm $FRONTEND_IMAGE >/dev/null 2>&1 || true
#             docker run -d \
#               --name $FRONTEND_IMAGE \
#               -p $FRONTEND_PORT:80 \
#               $FRONTEND_IMAGE || { echo "❌ Frontend deployment failed"; exit 1; }

#             # ========== 后端处理阶段 ==========
#             echo "📂 Moving to backend directory..."
#             cd ../wuyu-server  # 从项目根目录进入wuyu-server
#             pwd  # 打印当前工作目录，用于调试
            
#             # 停止当前端口运行的Java应用
#             echo "🛑 Checking for existing processes on port $SERVER_PORT..."
#             PID=$(lsof -t -i:$SERVER_PORT || true)
#             if [ ! -z "$PID" ]; then
#               echo "🛑 Stopping existing backend process (PID: $PID)..."
#               kill -9 $PID || true
#               # 等待进程完全停止
#               sleep 3
#             fi
            
#             # 使用Maven构建项目（跳过测试）
#             echo "🔧 Compiling backend with Maven..."
#             mvn clean package -DskipTests || { echo "❌ Backend build failed"; exit 1; }
            
#             # 重命名JAR文件
#             mv target/*.jar target/$BACKEND_JAR_NAME
            
#             # 清理旧日志
#             rm -f $BACKEND_LOG
            
#             # 启动JAR包（后台运行）
#             echo "🚀 Starting backend: java -jar target/$BACKEND_JAR_NAME --server.port=$SERVER_PORT"
#             nohup java -jar target/$BACKEND_JAR_NAME --server.port=$SERVER_PORT > $BACKEND_LOG 2>&1 &
            
#             # 记录启动进程ID
#             BACKEND_PID=$!
#             echo "📝 Backend started with PID: $BACKEND_PID"
            
#             # 验证后端启动 - 使用更健壮的检查机制
#             echo "⏳ Waiting for backend to start on port $SERVER_PORT..."
#             MAX_RETRIES=10
#             RETRY_DELAY=3
            
#             for i in $(seq 1 $MAX_RETRIES); do
#               sleep $RETRY_DELAY
              
#               # 检查进程是否仍在运行
#               if ! ps -p $BACKEND_PID > /dev/null; then
#                 echo "❌ Backend process (PID: $BACKEND_PID) has exited!"
#                 echo "===== BEGIN BACKEND LOGS ====="
#                 cat $BACKEND_LOG
#                 echo "===== END BACKEND LOGS ====="
#                 exit 1
#               fi
              
#               # 检查端口是否在监听
#               if lsof -i :$SERVER_PORT | grep LISTEN; then
#                 echo "✅ Port $SERVER_PORT is now listening"
#                 break
#               fi
              
#               echo "⌛ Attempt $i/$MAX_RETRIES: Port not ready yet..."
              
#               if [ $i -eq $MAX_RETRIES ]; then
#                 echo "❌ Backend failed to start on port $SERVER_PORT after $MAX_RETRIES attempts"
#                 echo "===== BEGIN BACKEND LOGS ====="
#                 cat $BACKEND_LOG
#                 echo "===== END BACKEND LOGS ====="
                
#                 # 尝试获取详细的错误信息
#                 echo "===== NETSTAT OUTPUT ====="
#                 netstat -tuln
#                 echo "===== PROCESS LIST ====="
#                 ps aux | grep java
#                 exit 1
#               fi
#             done
            
#             # 健康检查端点验证
#             echo "🩺 Performing health check..."
#             HEALTH_CHECK_URL="http://localhost:$SERVER_PORT/actuator/health"
#             if ! curl -s --retry 5 --retry-delay 2 $HEALTH_CHECK_URL >/dev/null; then
#               echo "❌ Health check failed at $HEALTH_CHECK_URL"
#               echo "===== HEALTH CHECK RESPONSE ====="
#               curl -i $HEALTH_CHECK_URL || true
#               exit 1
#             fi
            
#             echo "✅ Backend started successfully on port $SERVER_PORT"

#             # ========== 完成阶段 ==========
#             echo "✅ Deployment completed for main branch"
#             echo "Backend: $BACKEND_JAR_NAME running on port $SERVER_PORT (PID: $BACKEND_PID)"
#             echo "Frontend: $FRONTEND_IMAGE @ port $FRONTEND_PORT"
