<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fiveup.core.noticeBooklet.mapper.ExportZipMapper">

    <select id="getByIds" resultType="com.fiveup.core.noticeBooklet.domain.vo.NoticeBookletVo">
        select s.student_name, s.student_class_number,  s.student_grade,
        ds.s_deyu, ds.s_zhiyu, ds.s_tiyu, ds.s_meiyu, ds.s_laoyu,
        dp.p_deyu, dp.p_zhiyu, dp.p_tiyu, dp.p_meiyu, dp.p_laoyu,
        dc.comment
        from fiveup.di_student s
        left join fiveup.di_studentscore ds on s.student_id = ds.student_id
        left join fiveup.di_studentplan dp on dp.p_id = s.student_id
        left join fiveup.di_studentplancomment dc on dc.p_id = s.student_id
        where s.student_id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </select>

</mapper>